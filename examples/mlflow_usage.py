"""
`mlflow ui` should be executed from the directory where the mlruns directory is located.
    - You should execute the mlflow ui command from the directory one level above the mlruns directory. This is because the MLflow UI will look for the mlruns directory in the parent directory to load the corresponding runs.
"""

# Test out with a dummy directory.
